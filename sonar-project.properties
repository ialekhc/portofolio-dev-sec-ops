# Sonar project identifiers
# `sonar.projectKey` can stay here for clarity; CI may override via secrets.
sonar.projectKey=Alekh
sonar.organization=
sonar.projectName=Portfolio DevQAOps

# Source settings
sonar.sources=.
sonar.sourceEncoding=UTF-8

# Exclusions
sonar.exclusions=node_modules/**,dist/**,build/**,.github/**,cloudformation.yaml
